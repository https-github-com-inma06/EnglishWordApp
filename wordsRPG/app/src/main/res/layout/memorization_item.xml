<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="adapter"
            type="com.uhavecodingproblem.wordsrpg.component.MemorizationViewPagerAdapter" />

        <variable
            name="data"
            type="com.uhavecodingproblem.wordsrpg.data.WordData" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/word_count"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:gravity="center"
            android:textStyle="bold"
            app:autoSizeTextType="uniform"
            app:layout_constraintHeight_percent="0.1"
            app:layout_constraintTop_toTopOf="parent" />


        <TextView
            android:id="@+id/word"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:text="@{adapter.hideWordCheck == false ? data.word : null}"
            android:background="@color/colorPrimary"
            android:gravity="center"
            android:textColor="@color/colorWhite"
            android:textStyle="bold"
            app:autoSizeTextType="uniform"
            app:layout_constraintHeight_percent="0.6"
            app:layout_constraintTop_toBottomOf="@id/word_count" />

        <TextView
            android:id="@+id/mean"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:text="@{adapter.hideMeanCheck == false ? data.mean : null}"
            android:gravity="center"
            android:textStyle="bold"
            app:autoSizeTextType="uniform"
            app:layout_constraintHeight_percent="0.15"
            app:layout_constraintTop_toBottomOf="@id/word" />
        
        <ImageView
            android:id="@+id/word_mic"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:src="@drawable/ic_baseline_mic_24"
            android:scaleType="fitCenter"
            app:layout_constraintHeight_percent="0.15"
            app:layout_constraintTop_toBottomOf="@id/mean"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>